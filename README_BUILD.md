# 📦 دليل بناء التطبيق كملف EXE

## المتطلبات

1. **Python 3.7+** مثبت على النظام
2. **PyInstaller** مثبت:
   ```bash
   pip install pyinstaller
   ```

## طريقة البناء

### الطريقة السريعة (موصى بها):

1. **تشغيل ملف البناء:**
   ```bash
   build.bat
   ```

2. **انتظار اكتمال البناء** (قد يستغرق بضع دقائق)

3. **الملف النهائي:**
   - الموقع: `dist\نظام_الحضور_والانصراف.exe`
   - يمكن نسخه وتشغيله على أي جهاز Windows بدون الحاجة إلى Python

### الطريقة اليدوية:

```bash
pyinstaller --clean --noconfirm build_exe.spec
```

## الملفات المولدة

بعد البناء، ستجد:

- **dist/**: يحتوي على الملف التنفيذي النهائي
- **build/**: ملفات البناء المؤقتة (يمكن حذفها)
- ***.spec**: ملف إعدادات PyInstaller

## ملاحظات مهمة

1. **الأيقونة:**
   - يتم استخدام `icon.ico` تلقائياً
   - تأكد من وجود الملف في نفس المجلد

2. **الحجم:**
   - حجم الملف النهائي قد يكون كبيراً (حوالي 100-200 MB)
   - هذا طبيعي لأنه يحتوي على Python وجميع المكتبات المطلوبة

3. **الأداء:**
   - أول تشغيل قد يكون أبطأ قليلاً
   - التشغيلات اللاحقة ستكون أسرع

4. **التوافق:**
   - يعمل على Windows 10/11
   - لا يحتاج إلى Python مثبت على الجهاز المستهدف

## استكشاف الأخطاء

### خطأ: "PyInstaller not found"
```bash
pip install pyinstaller
```

### خطأ: "Module not found"
تأكد من تثبيت جميع المكتبات:
```bash
pip install PyQt5 pandas openpyxl
```

### خطأ: "Icon not found"
تأكد من وجود `icon.ico` في نفس المجلد

## التوزيع

بعد البناء، يمكنك:
1. نسخ `dist\نظام_الحضور_والانصراف.exe` إلى أي جهاز
2. تشغيله مباشرة بدون أي إعدادات إضافية
3. مشاركته مع المستخدمين الآخرين

---

**المطور:** علي إبراهيم مصطفى  
**الإصدار:** 1.0

